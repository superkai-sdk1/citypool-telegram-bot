{
  "name": "citypool",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --exec ts-node src/index.ts",
    "start": "node dist/index.js",
    "clean": "rmdir /S /Q dist 2>nul || true",
    "rebuild": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["telegram", "bot", "poll", "scheduler"],
  "author": "",
  "license": "ISC",
  "description": "Telegram bot for scheduling regular polls in groups",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.4",
    "@types/node-cron": "^3.0.11",
    "@types/sqlite3": "^3.1.11",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "dotenv": "^16.6.0",
    "node-cron": "^4.1.1",
    "sqlite3": "^5.1.7",
    "telegraf": "^4.16.3"
  }
}
