#!/bin/bash
# Скрипт для деплоя на Render

echo "🚀 Начинаю деплой CityPool Bot..."

# Устанавливаем зависимости
echo "📦 Установка зависимостей..."
npm install

# Собираем проект
echo "🔨 Сборка проекта..."
npm run build

# Проверяем успешность сборки
if [ $? -eq 0 ]; then
    echo "✅ Сборка успешна!"
    echo "🤖 Бот готов к запуску"
else
    echo "❌ Ошибка при сборке"
    exit 1
fi
